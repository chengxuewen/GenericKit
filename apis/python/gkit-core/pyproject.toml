[build-system]
requires = ["maturin>=1.8,<2.0"]
build-backend = "maturin"

[project]
name = "gkit_core"
version = "0.1.0"
requires-python = ">=3.8"

[tool.maturin]
features = ["extension-module"]
module-name = "gkit_core._gkit_core"
