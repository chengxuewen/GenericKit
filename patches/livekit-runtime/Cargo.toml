[package]
name = "livekit-runtime"
version = "0.4.0"
edition = "2024"

[dependencies]
tokio = { version = "1", default-features = false, features = ["rt", "rt-multi-thread", "net", "time"] }
tokio-stream = "0.1"
